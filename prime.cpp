#include<bits/stdc++.h>
using namespace std;

void primeSieve(int n) {
    bool prime[n+1] = {true};
    for(int i = 0; i <= sqrt(n); ++i) {
        if(prime[i]) {
            for(int j = )
        }
    }
}

int main() {
    int n = 100000000;

    for(int i = 0; i < n; ++i) {
        if(prime[i]) {
            for(int j = )
        }
    }
    return 0;
}
